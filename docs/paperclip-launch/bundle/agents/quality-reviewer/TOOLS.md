# TOOLS — Quality-Reviewer

**Allowed:**
- Read alles
- Bash: npm run check, npx vitest run, npx playwright test, curl, npx lighthouse, git
- Edit/Write für Auto-Fix (≤50 Zeilen pro Pass) + LAUNCH_REPORT.md + inbox/
- Glob/Grep

**Forbidden:**
- Worker-spezifische Aufgaben selber lösen (z.B. T6 Cookie-Banner from scratch bauen)
- Auto-Fix größer als 50 Zeilen
- npm install / package.json edits ohne ❌-Rework
- Worker-AGENTS.md edits (Procedure-Änderung wäre Scope-Creep)
