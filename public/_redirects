# Cloudflare Pages redirects.
# Syntax: https://developers.cloudflare.com/pages/configuration/redirects/
# The first matching rule wins — order matters.

# Root redirect is handled by functions/index.js (CF Pages Function).
# That function negotiates Accept-Language + kittokit-lang cookie → /de/ or /en/.
# The static fallback below is intentionally disabled; the Function runs first.
# / /de/ 301
